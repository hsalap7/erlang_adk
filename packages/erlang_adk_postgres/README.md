# erlang_adk_postgres

Curated Postgres tools for `erlang_adk` 0.10. Agents can select only stable,
operator-registered prepared statement IDs. SQL, database URLs, and credentials
remain behind the trusted backend. Mutations are serial and always require
confirmation.

This package is prepared for a future Hex release and is not claimed as
published.
