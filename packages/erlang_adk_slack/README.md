# erlang_adk_slack

Curated Slack tools for `erlang_adk` 0.10. The package wraps a strict,
precompiled `adk_openapi_toolset`; any operation not covered by the package
manifest rejects the connector catalog. Posting a message is serial and always
requires confirmation.

This package is prepared for a future Hex release and is not claimed as
published.
