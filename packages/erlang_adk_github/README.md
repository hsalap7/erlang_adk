# erlang_adk_github

Curated GitHub tools for `erlang_adk` 0.10. An operator-created MCP client is
injected at runtime; agent configuration contains only a trusted toolset ID.
Repository reads are parallel-safe, while issue creation is serial and always
requires confirmation.

This package is prepared for a future Hex release and is not claimed as
published.
