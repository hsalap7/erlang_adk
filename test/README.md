# Erlang test layout

The canonical ownership map and recursive-discovery rules are in
[`docs/TEST_LAYOUT.md`](../docs/TEST_LAYOUT.md). This local pointer keeps the
convention visible while browsing `test/`; the release package excludes the
test source tree and includes the canonical guide.
