The Erlang ADK release was rendered with immutable image:
  {{ include "erlang-adk.image" . }}

Replica count is {{ .Values.replicaCount }}. Public Service and Ingress are
disabled by default; enabling them requires an authenticated deployment-owned
HTTP adapter and TLS configuration.

No live-cluster or provider smoke is implied by a successful Helm render.
